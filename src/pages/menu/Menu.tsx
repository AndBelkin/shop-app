import type { FC } from "react";
import PromoList from "../../layouts/promo/PromoList";

const Menu: FC = () => {
  return (
    <main className="menu">
      <PromoList />
    </main>
  );
};

export default Menu;
